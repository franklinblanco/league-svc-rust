
//Loop through all the player profile fields and find which of these has a value
//An update should send all fields changed or didn't change
//Therefore the macro should iterate through all the fields of the optional object
//Then look for those fields in the current object. If there's any change store them into a map

macro_rules! _iterate_fields {
    () => {
        
    };
}